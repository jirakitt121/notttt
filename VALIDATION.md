# ผลทดสอบ SQLite + OOP + Workflow

วันที่ตรวจ: 3 กันยายน 2026

| รายการ | ผล |
| --- | --- |
| TypeScript (`npm run check`) | ผ่าน |
| สร้างเว็บสำหรับใช้งานจริง (production build) | ผ่าน |
| ฐานข้อมูลว่าง 13 ตาราง ตรงกับ migrations ของเว็บ | ผ่าน |
| รัน schema.sql เพื่อสร้างฐานข้อมูลใหม่ | ผ่าน |
| PRAGMA integrity_check / foreign_key_check | ผ่าน |
| ไม่มีข้อมูลผู้ใช้หรือข้อมูลลับในฐานข้อมูลที่ส่งมอบ | ผ่าน — ทุกตารางว่าง |
| ดัชนี inspections_part ถูกใช้ใน query plan ที่ทดสอบ | ผ่าน |
| OOP: ตรวจข้อมูล ซ่อนค่า และเปลี่ยน store สำหรับทดสอบ | ผ่าน |
| เพิ่ม แก้ไข อ่าน รหัสซ้ำ และรายการไม่พบ | ผ่าน |
| แยกข้อมูลจริงและสาธิต | ผ่าน |
| ข้อความลักษณะ SQL injection ถูกเก็บเป็นข้อมูล | ผ่าน |
| การเพิ่มข้อมูลสาธิตสำเร็จ/ย้อนกลับเป็นชุดเดียวกัน | ผ่าน |
| ข้อมูลคงอยู่หลังปิดและเปิด SQLite ใหม่ | ผ่าน |
| API เดิม: รูปภาพ snapshot ข้อมูลน็อต รีวิว CSV/ZIP | ผ่าน |
| แอดมิน: รหัสผ่าน เซสชัน สิทธิ์ CSRF และออกจากระบบ | ผ่าน |
| แชทไม่ใช้คีย์: แหล่งข้อมูล ขอบเขต ประวัติ และกรณีผิดพลาด | ผ่าน |
| ชุดส่งมอบแยกโฟลเดอร์ (`node tests/test_bundle.mjs`) | ตัวจัดแพ็กเกจตรวจและยอมสร้างชุดส่งมอบต่อเมื่อผ่าน |

## วิธีทดสอบซ้ำ

ในโปรเจกต์เต็ม ใช้ `npm run check`, `npm run test:architecture` และ `npm run test:api`

ใน ZIP นี้ ใช้ `node tests/test_bundle.mjs` โดยต้องมี Node.js 22.13 ขึ้นไป ไม่ต้องติดตั้ง package เพิ่ม และไม่เชื่อมต่อบริการภายนอก

## ข้อจำกัด

- ใช้ข้อมูลทดสอบและฐานข้อมูลชั่วคราว ไม่ทดสอบด้วยบัญชี/ข้อมูลการตรวจจริงของผู้ใช้
- ไม่ได้ทดสอบผ่านเบราว์เซอร์หรืออุปกรณ์จริงในรอบนี้
- ผัง PNG ตรวจการจัดวางและความอ่านง่ายแล้ว ไม่ใช่ภาพหน้าจอเว็บ
- ยังไม่ได้วัดความแม่นยำกับโมเดลที่ผู้ใช้เทรนจริง และไม่ได้เพิ่มโมเดลตรวจชิ้นงานจริง
- งานนี้เปลี่ยนโครงสร้างโค้ดส่วนรายการน็อต ไม่มี migration ใหม่ ไม่มีการย้ายหรือลบฐานข้อมูลเว็บ
- SQLite ใน ZIP ไม่ซิงก์กับ D1 และไม่ใช่ระบบล็อกอินหรือเว็บเต็มที่พร้อม deploy แยกต่างหาก
